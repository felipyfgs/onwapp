export enum WAPrivacyValue {
  ALL = 'all',
  CONTACTS = 'contacts',
  CONTACT_BLACKLIST = 'contact_blacklist',
  NONE = 'none',
}

export enum WAPrivacyOnlineValue {
  ALL = 'all',
  MATCH_LAST_SEEN = 'match_last_seen',
}

export enum WAPrivacyCallValue {
  ALL = 'all',
  KNOWN = 'known',
}

export enum WAPrivacyMessagesValue {
  ALL = 'all',
  CONTACTS = 'contacts',
}

export enum WAReadReceiptsValue {
  ALL = 'all',
  NONE = 'none',
}

export enum WAPrivacyGroupAddValue {
  ALL = 'all',
  CONTACTS = 'contacts',
  CONTACT_BLACKLIST = 'contact_blacklist',
}
