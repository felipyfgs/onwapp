# Development Stack - Dependencies only (API runs locally)
# Usage: make up (or docker compose up -d)
# Note: Uses different container/volume names to avoid conflicts with Swarm stack

services:
  # PostgreSQL Database
  dev_postgres:
    image: postgres:16-alpine
    container_name: zpwoot-dev-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      TZ: America/Sao_Paulo
      POSTGRES_USER: ${POSTGRES_USER:-zpwoot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-zpwoot123}
      POSTGRES_DB: ${POSTGRES_DB:-zpwoot}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - dev_postgres_data:/var/lib/postgresql/data
    networks:
      - zpwoot-dev-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-zpwoot} -d ${POSTGRES_DB:-zpwoot}']
      interval: 10s
      timeout: 5s
      retries: 5

  # NATS JetStream
  dev_nats:
    image: nats:latest
    container_name: zpwoot-dev-nats
    restart: unless-stopped
    ports:
      - '4222:4222'
      - '8222:8222'
    environment:
      TZ: America/Sao_Paulo
    command:
      - "--http_port=8222"
      - "--js"
      - "--store_dir=/data"
    volumes:
      - dev_nats_data:/data
    networks:
      - zpwoot-dev-network

  # MinIO (S3 Storage)
  dev_minio:
    image: quay.io/minio/minio:RELEASE.2024-01-13T07-53-03Z-cpuv1
    container_name: zpwoot-dev-minio
    command: server /data --console-address ":9001"
    restart: unless-stopped
    ports:
      - '9000:9000'
      - '9001:9001'
    environment:
      TZ: America/Sao_Paulo
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-zpwoot}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-zpwoot123}
    volumes:
      - dev_minio_data:/data
    networks:
      - zpwoot-dev-network

  # DBGate (Database Manager - pre-configured)
  dev_dbgate:
    image: dbgate/dbgate:latest
    container_name: zpwoot-dev-dbgate
    restart: unless-stopped
    ports:
      - '3100:3000'
    environment:
      TZ: America/Sao_Paulo
      CONNECTIONS: con1
      LABEL_con1: zpwoot PostgreSQL Dev
      SERVER_con1: dev_postgres
      USER_con1: ${POSTGRES_USER:-zpwoot}
      PASSWORD_con1: ${POSTGRES_PASSWORD:-zpwoot123}
      PORT_con1: 5432
      ENGINE_con1: postgres@dbgate-plugin-postgres
      DATABASE_con1: ${POSTGRES_DB:-zpwoot}
    depends_on:
      dev_postgres:
        condition: service_healthy
    networks:
      - zpwoot-dev-network

  # Webhook Tester (for testing webhooks in development)
  dev_webhook:
    image: ghcr.io/tarampampam/webhook-tester:2
    container_name: zpwoot-dev-webhook
    restart: unless-stopped
    ports:
      - '8090:8080'
    environment:
      TZ: America/Sao_Paulo
      LOG_LEVEL: debug
      AUTO_CREATE_SESSIONS: "true"
      MAX_REQUESTS: "512"
    networks:
      - zpwoot-dev-network

volumes:
  dev_postgres_data:
  dev_nats_data:
  dev_minio_data:

networks:
  zpwoot-dev-network:
    driver: bridge
